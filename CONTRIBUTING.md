# Contributing

Use Python 3.8-compatible syntax and preserve executor protocol behavior. Create `.venv`, install the `dev` extra, then run Ruff, Mypy, the documentation check, and the full test suite before submitting a change. Add tests for externally visible behavior and keep English and Chinese documentation paired.

Please keep changes focused and do not commit secrets, generated distributions, virtual environments, or managed logs.
