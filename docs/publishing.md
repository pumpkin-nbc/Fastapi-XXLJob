# Publishing

Update the single version source and both changelogs, run every quality gate, then build the wheel and source archive. Inspect both archives and install the wheel in a fresh Python 3.8 virtual environment before publishing.

This repository does not publish, tag, or push automatically. Maintainers must review Apache-2.0 metadata and choose the target package index explicitly.
