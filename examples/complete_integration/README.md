# Complete integration example

This example supplies all optional handlers and enables lifespan registration. Replace every address and secret before running it.

```bash
uvicorn examples.complete_integration.app:app --host 0.0.0.0 --port 8000
```
